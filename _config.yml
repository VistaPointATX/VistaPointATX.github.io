remote_theme: "mmistakes/minimal-mistakes@4.22.0"
plugins:
  - jekyll-include-cache
defaults:
  # _pages
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      author_profile: false
